import { BaseBlockstore } from 'blockstore-core/base'

class DataAgentStore extends BaseBlockstore {
  put (key, val, options) {
    // store a block
  }

  get (key, options) {
    // retrieve a block
  }

  // ...etc
}